input AgeRangeFilter{
    minAge: Int!
    maxAge: Int!
}

type Customer{
    id: ID
    name: String
    age: Int
    city: String
    orders(limit: Int!): [CustomerOrder]!
}

type CustomerOrder{
    id: ID!
    description: String
}